% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/idu.R
\name{get_etalab_data_by_idu}
\alias{get_etalab_data_by_idu}
\title{Retrieve Etalab cadastral data for given IDUs}
\usage{
get_etalab_data_by_idu(idu, layer = NULL, verbose = TRUE)
}
\arguments{
\item{idu}{A character vector of IDU codes (14-character format).
All codes are validated before data retrieval.}

\item{layer}{Optional character string specifying the cadastral layer to retrieve.
If \code{NULL}, all available layers are returned.}

\item{verbose}{Logical. If \code{TRUE}, prints progress messages during data retrieval.}
}
\value{
The cadastral data retrieved from Etalab for the communes
corresponding to the provided IDUs.
The return type depends on \code{get_etalab()}:
typically an \code{sf} or \code{data.frame} object.
}
\description{
This internal function validates and processes one or more cadastral parcel
identifiers (IDUs), extracts the corresponding INSEE commune codes, and
retrieves the matching cadastral data from Etalab.
}
\details{
This function ensures that all provided IDUs are valid using \code{\link[=idu_check]{idu_check()}},
extracts their components via \code{\link[=idu_split]{idu_split()}}, and determines the relevant
INSEE commune codes to pass to \code{\link[=get_etalab]{get_etalab()}}.
}
\examples{
\dontrun{
# Retrieve data for a single IDU
get_etalab_data_by_idu("72181000AB0001")

# Retrieve data for multiple IDUs within the same commune
get_etalab_data_by_idu(
  c("72181000AB0001", "72181000AB0002"),
  layer = "parcelles"
)
}

}
\keyword{internal}
